{% extends 'base.html.twig' %}

{% block title %}Testimony index{% endblock %}

{% block body %}
    <h1>Témoignage index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Description</th>
                <th>Date</th>
                <th>Firtsname</th>
                <th>Lastname</th>
                <th>Age</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for testimony in testimonies %}
            <tr>
                <td>{{ testimony.id }}</td>
                <td>{{ testimony.title }}</td>
                <td>{{ testimony.description }}</td>
                <td>{{ testimony.date ? testimony.date|date('Y-m-d') : '' }}</td>
                <td>{{ testimony.firtsname }}</td>
                <td>{{ testimony.lastname }}</td>
                <td>{{ testimony.age }}</td>
                <td>
                    <a href="{{ path('testimony_show', {'id': testimony.id}) }}">show</a>
                    <a href="{{ path('testimony_edit', {'id': testimony.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('testimony_new') }}">Create new</a>
{% endblock %}

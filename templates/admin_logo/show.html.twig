{% extends 'base.html.twig' %}

{% block title %}Logo{% endblock %}

{% block body %}
    <h1>Logo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ logo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ logo.Name }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ logo.Logo }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ logo.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('logo_index') }}">back to list</a>

    <a href="{{ path('logo_edit', {'id': logo.id}) }}">edit</a>

    {{ include('logo/_delete_form.html.twig') }}
{% endblock %}

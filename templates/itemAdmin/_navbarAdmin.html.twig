<div class="navAdmin">
    <nav class="navbar navbar-expand-lg  navbar-light bg-transparent">
        <a class="navbar-brand" href="#">Espace Administrateur</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav nav-page w-100 d-flex justify-content-center">
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{ path("admin_home") }}">Accueil<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{ path("admin_event_index") }}">Actualités</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{ path("admin_index_speciality") }}">Spécialités</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="#">Témoignages</a>
                </li>
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link text-dark" href="{{ path('app_logout') }}"> Se déconnecter</a>
                    {% else %}
                        <a class="nav-link text-dark" href="{{ path('app_login') }}"> Se connecter</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </nav>
</div>


{% block javascripts %}
    {{ encore_entry_script_tags('navbarAdmin') }}
{% endblock %}
